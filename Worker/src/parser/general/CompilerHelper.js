
function scanResult(tLeft, tResult){
    let cadena = "";
    if(tResult = "scan()"){
        
    }else{
        cadena += tLeft +" = "+tResult+" ;\n";
    }
    return cadena;
}

function typeCheck(tWord){
    let cadena = "";
    let aux = tWord.toString().split("");
    for(let index=0; index<aux.length; index++){
        let character = aux[index];
        
    }
}


module.exports = {
    isCompiler
}